// AboutIntro is now merged into AboutFull — this file is kept for routing compatibility
// but renders nothing (the AboutFull section has the full bio + intro)
const AboutIntro = () => null;
export default AboutIntro;
